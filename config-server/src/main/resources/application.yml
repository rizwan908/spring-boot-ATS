server:
  port: 0

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rizwan908/config-server
          clone-on-start: true